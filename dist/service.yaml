apiVersion: v1
kind: Service
metadata:
  name: microservice-auth-web-service
  namespace: 'microservices-namespace'
  labels:
    moduleid: microservice-auth-web-service
    version: 1.70.1-master
spec:
  type: NodePort
  selector:
    app: microservice-auth-web-deployment
  ports:
    -
      name: microservice-auth-web-port
      port: 3000
